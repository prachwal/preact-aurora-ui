import { DocsPage } from '../pages';

/**
 * DocsLayout - Shows DocsHomepage directly on the main page
 * No routing needed - just displays the documentation homepage
 */
export function DocsLayout() {
  return <DocsPage />;
}
